const express = require('express');
const router = express.Router();

const {createToken, postStk} = require('../controllers/controller');

router.post('/stkpush',createToken, postStk);


module.exports = router;
